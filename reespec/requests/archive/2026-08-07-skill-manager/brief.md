# Skill manager extension

A skill-loading pi extension that gives the agent selective access to skills — a small permanent set from config, plus on-demand ephemeral loading with TTL-based expiry, catalog browsing, and persistence across restarts.
